import { createStore } from 'vuex';
import modalModule from './modules/modal';

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: { modalModule },
});
